206. Reverse Linked List
var reverseList = function(head) {
    let curr = head;
    let prev = null;
    let res = [];
    while(curr !== null){
        let temp = curr.next;
        curr.next = prev;
        prev = curr;
        curr = temp;
    }
    return prev;
};
